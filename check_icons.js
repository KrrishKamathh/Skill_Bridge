const lucide = require('lucide-react');
console.log('Available icons:', Object.keys(lucide).filter(k => k.toLowerCase().includes('twitter') || k.toLowerCase().includes('github') || k.toLowerCase().includes('linkedin')));
